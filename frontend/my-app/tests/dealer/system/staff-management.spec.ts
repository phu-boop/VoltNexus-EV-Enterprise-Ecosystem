import { test, expect } from '@playwright/test';

test.describe('Dealer Staff Management', () => {

    test.beforeEach(async ({ page }) => {
    });

    test('should be able to create, edit and list dealer staff accounts', async ({ page }) => {
    });
});
