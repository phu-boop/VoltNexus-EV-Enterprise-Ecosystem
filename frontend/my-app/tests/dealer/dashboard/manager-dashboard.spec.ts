import { test, expect } from '@playwright/test';

test.describe('Dealer Manager Dashboard', () => {

    test.beforeEach(async ({ page }) => {
    });

    test('should load the manager dashboard correctly', async ({ page }) => {
    });
});
