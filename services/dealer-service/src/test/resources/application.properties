spring.application.name=dealer-service
server.port=8083

# DATABASE
spring.datasource.url=jdbc:mysql://vms-commerce14-tmdtvmscommerce.i.aivencloud.com:25858/dealer_db?sslMode=REQUIRED&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASS_DEALER:AIVEN_PASS_SECRET}

SPRING_DATASOURCE_URL=${spring.datasource.url}
SPRING_DATASOURCE_USERNAME=avnadmin
SPRING_DATASOURCE_PASSWORD=${DB_PASS_DEALER:AIVEN_PASS_SECRET}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# KAFKA
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=dealer-service-group
